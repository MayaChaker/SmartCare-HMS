# Server
PORT=5000
NODE_ENV=development

# Authentication
# Generate a secure random string and replace this value in your local .env
JWT_SECRET=replace_with_secure_random_string

# Database (Sequelize / MySQL)
# Match these to your local setup. DB_NAME can be 'smartcare_db' to align with your current .env
DB_NAME=smartcare_db
DB_USER=root
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=3306
DB_DIALECT=mysql

# Notes:
# - Keep your real secrets in backend/.env (it is gitignored).
# - Ensure MySQL is running and the database exists: smartcare_db
# - You can change NODE_ENV to 'production' in deployments.